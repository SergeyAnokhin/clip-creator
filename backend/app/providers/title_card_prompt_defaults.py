"""Seed value for the editable settings.title_card_base_prompt, sent on every
real `title_card/generate` call ahead of the quoted title/author text block
`title_card.py` appends (same seam as `scenes_prompt_defaults.py`'s base
prompts / `suno_prompt_defaults.py`'s base prompt - a general creative brief
the caller's own text is appended to, not a template with placeholders)."""

DEFAULT_TITLE_CARD_BASE_PROMPT = """\
Ты — дизайнер обложек и афиш. Тебе даны несколько референсных изображений — \
используй их визуальный стиль (палитру, освещение, текстуры, художественную \
манеру) как основу.

Задача: используя именно этот стиль, создай типографскую композицию с \
текстом, приведённым ниже. Текст должен выглядеть как часть единой картины \
референсов — тем же художественным приёмом (например, если референсы \
рисованные — буквы тоже нарисованы в той же технике; если это фотографии с \
характерным светом — буквы стилизованы под тот же свет и текстуру).

Требования:
- Никаких других сюжетных элементов, кроме текста и лёгких декоративных \
деталей в том же стиле — это не новая сцена, а типографский постер.
- Фон — сплошной чёрный или прозрачный, без градиентов и посторонних объектов.
- Горизонтальная композиция, текст хорошо читается, крупный и центрирован.
- Сохраняй кавычки вокруг каждой строки текста как часть художественного \
оформления, если это уместно стилю.

Текст для афиши:"""
